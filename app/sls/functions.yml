functions:
  ip:
    runtime: provided.al2023
    handler: bootstrap
    architecture: arm64
    memorySize: 2048
    timeout: 20
    package:
      individually: true
      artifact: ./artifact/function.zip
    events:
      - http:
          private: true
          path: /ip
          method: get
          cors: true